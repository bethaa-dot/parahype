# ParaHype (JavaScript)

JavaScript version of ParaHype — React + Node/Express + MongoDB.

## Run locally

```bash
npm install
cd client && npm install
cd ../server && npm install
cd ..
npm run dev
```

Client: http://localhost:5173

Server: http://localhost:5000
